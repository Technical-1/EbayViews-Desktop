[project]
name = "ebayviews-desktop"
version = "0.1.0"
description = "Desktop UI for controlled eBay item request experiments"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
